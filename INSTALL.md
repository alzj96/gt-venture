# 安装与首次跑通

三个宿主的装法不同，但技能文件是同一套，不用改。

**装完请照着最后一节「首次跑通清单」验一遍**——那五项只有真机能验，而且每一项失败的方式都是静默的（不报错，行为不对）。

---

## WorkBuddy

### 装

1. 下载 `gt-venture.zip`
2. 打开 WorkBuddy → 技能 → **添加技能** → **上传技能**
3. 把 zip 拖进去，或点「选择文件」

官方文档说导入后自动完成配置，无需额外操作。

**另一条路**：把整个文件夹放进 `~/.workbuddy/skills/` 然后重启。

### 装完先配一件事：命令白名单

这个技能要调四个 Python 脚本。**默认权限下每次调用都会弹确认**，一次诊断弹十几次，没法用。

去 **设置 → 安全中心 → 命令安全**，把脚本路径前缀加进放行名单。

**不要为此开「完全访问权限」**——那是全局开关，官方明确不建议日常开启，而且它放开的是文件、终端、网络三样，远超这个技能需要的。

### 工作空间

新建任务时选一个目录，诊断档案会写在那里的 `创业档案/`。

**给每个人单独一个目录。** 如果你在给多个客户做诊断，模式和敏感问题记录会串——脚本会在发现多个项目时提醒你，但别依赖提醒。

---

## 豆包 PC 版

**技能·连接器·伙伴** → **我的技能** → 右上角 **新建 → 上传技能**。

⚠️ **豆包能不能写文件到工作空间，我们还没实测。** 写不了的话，档案功能失效——而档案是这个技能唯一的记忆，没有它，用户第十次来和第一次来没区别。

装完第一件事就是验这个（见清单第 2 项）。验不过就告诉我们，我们把豆包移出支持列表，而不是让你用一个半残的版本。

---

## Claude Code

```bash
mkdir -p ~/.claude/skills
cd ~/.claude/skills
git clone https://github.com/alzj96/gt-venture.git
```

或者放进项目里的 `.claude/skills/`。

`allowed-tools` 里已经排除了 Edit——这个技能不写代码，从劝告变成了约束。

---

## 首次跑通清单

**装完跑一遍，五项全过才算装好。** 每一项失败都是静默的。

把 `<技能目录>` 换成 SKILL.md 实际所在的绝对路径，`<工作空间>` 换成你的任务目录。

### 1. 脚本能跑，档案落在对的地方

```bash
cd <工作空间>
python3 <技能目录>/scripts/archive.py --workspace "$(pwd)" find
```

**要看到**：打印的查找位置是**你的工作空间**，不是技能安装目录。

对不上就是最常见的那个坑：读的人以为要先 `cd` 到技能目录。**别 cd**，用绝对路径调脚本 + `--workspace` 传工作空间。

### 2. 能写文件（豆包重点验这项）

```bash
python3 <技能目录>/scripts/archive.py --workspace "$(pwd)" save --project "装机测试" --step 1 --answer "测试用"
ls 创业档案/
```

**要看到**：`创业档案/装机测试-诊断-<日期>.md` 真的在你的工作空间里。

写不出来 → 这个宿主不支持这个技能的核心功能。

### 3. 规则库读得到

```bash
python3 <技能目录>/scripts/check_rules.py
```

**要看到**：「✓ 有效 N 条」，以及下面那段「上面回答的是规则新不新，不是你这行覆盖没覆盖」的提示。

看不到规则库覆盖范围清单 → 文件没装全。

### 4. 命令白名单配对了（WorkBuddy）

连着跑三条命令，**数一下弹了几次确认**：

```bash
python3 <技能目录>/scripts/check_rules.py
python3 <技能目录>/scripts/archive.py --workspace "$(pwd)" find
python3 <技能目录>/scripts/pattern.py --workspace "$(pwd)" show
```

弹三次 → 白名单没配对，回去看「命令白名单」那节。
一次不弹 → 配好了。

### 5. 跑一遍自带的回归测试

```bash
python3 <技能目录>/scripts/test_scripts.py
```

**要看到**：`Ran 52 tests` + `OK`。

有失败的话，是宿主环境和我们的测试环境有差异——把失败的那几条发给我们，别自己改测试绕过去。

### 清理

```bash
rm -rf 创业档案/装机测试-诊断-*.md
```

---

## 开始用

装好之后直接说人话就行，不用记命令：

> 我想做一个给小学生练口算的小程序，想听听你的看法

它会先问你要**轻量筛查**（四到六轮，查三样最容易一票否决的东西）还是**全面诊断**（六问 + 合规闸门 + 竞品核实 + 报告）。

传统行业的话也能用：

> 我开了两家社区超市十一年了，想搞个小程序做社区团购，AI 能帮我做起来吗

---

## 装不上或者跑不对

跑通清单里哪一项失败，**把那一项的完整输出发出来**——包括命令和报错原文。

最常见的三个：

| 现象 | 原因 |
| --- | --- |
| `find` 说没找到档案，但你确实存过 | 上次调脚本时 `cd` 到技能目录了，档案写进了安装目录 |
| 每条命令都弹确认 | 命令白名单没配 |
| `python3: command not found` | 宿主没有 Python。技能有手工模式（见 `references/archive-format.md`），但会慢很多 |
